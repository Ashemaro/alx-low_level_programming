#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @r: Integer to be checked
 * Return: Returns nothing
 */

int _abs(int)
{
	int abs;

	if ( r < 0)
	{
		abs = 0-r;
		return (abs);
	}
	else
	{
		return (r);
	}
	return (0);
}
